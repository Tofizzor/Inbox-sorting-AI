We got backend to pull out email data and transform it into data that AI can understand.
Our mission here is to be the email "rules" - user does not need to define rules in order to get their emails sorted into correct folders.
Now these folders are important, so for AI to make correct predictions we need to come up with a solution.


We will expect email to come to an address - group that supports the applications

Title and body of the email are both as important - they might contain order number, error message or anything that could help support user to track the issue

Front end will handle a - urgent email pick up by the users and track the progress

First functionality is to get the email (mock email for now)

Process the email contents and form a message for AI to digest

Have a guide/doc for AI to use as the guide on what needs to be sorted where

Feed the guide and the processed email contents to AI and let it predict where the email belongs
