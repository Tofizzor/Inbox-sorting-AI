# Backend dependencies — install from backend/: pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.0.0

# Optional cloud LLM (not required for Ollama dev)
# openai>=1.0.0
